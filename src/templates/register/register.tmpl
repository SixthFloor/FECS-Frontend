<div class="container">
  <h1> Join SixthFloor !</h1>
  <div class="step">
    <div ui-view="step1"></div>
  </div>
  <div class="step">
    <div ui-view="step2"></div>
  </div>
  <div class="step">
    <div ui-view="step3"></div>
  </div>
  <div class="alert alert-danger "
  ng-show="!registerCtrl.valid">
    <strong>Alert!</strong> Invalid form
  </div>
  <div class="register-container">
    <button type="button" class="register-btn" ng-click="registerCtrl.submit()">Register</button>
  </div>
</div>
